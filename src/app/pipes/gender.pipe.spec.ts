/* tslint:disable:no-unused-variable */

import { GenderPipe } from './gender.pipe';

describe('Pipe: Gendere', () => {
  it('create an instance', () => {
    let pipe = new GenderPipe();
    expect(pipe).toBeTruthy();
  });
});
